meta {
  name: Un Register - Get by request Id
  type: http
  seq: 8
}

get {
  url: {{accountupdater_url}}/account-updates/{{saurreqId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{requestId}}
  merchant-id: {{merchantId}}
}

script:pre-request {
  // Fresh request-id per call
  bru.setVar("requestId", require("uuid").v4());
}

settings {
  encodeUrl: true
  timeout: 0
}