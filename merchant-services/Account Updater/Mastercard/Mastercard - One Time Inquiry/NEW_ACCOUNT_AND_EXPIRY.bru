meta {
  name: NEW_ACCOUNT_AND_EXPIRY
  type: http
  seq: 3
}

post {
  url: {{accountupdater_url}}/account-updates
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{$guid}}
  merchant-id: {{merchant-id}}
}

body:json {
  {
    "accountInformation": {
      "cardNumber": "5111111111111113",
      "expiry": {
        "month": 8,
        "year": 2026
      }
    }
  }
}

tests {
  // Set transactionId to variable for further use
  if(res.body.requestId) {
  bru.setEnvVar("naeotqreqId", res.body.requestId);
  }
  
  if(res.body.responseId) {
  bru.setEnvVar("naeotqrespId", res.body.responseId);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
