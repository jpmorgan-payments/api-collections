meta {
  name: NO_MATCH_PARTICIPATING_BIN
  type: http
  seq: 6
}

post {
  url: {{accountupdater_url}}/account-updates
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{request-id}}
  merchant-id: {{merchant-id}}
}

body:json {
  {
    "accountInformation": {
      "cardNumber": "5111111111111117",
      "expiry": {
        "month": 8,
        "year": 2026
      }
    },
    "cardAccountAction": "REGISTER",
    "merchantRecordIdentifier": "{{$guid}}"
  }
}

tests {
  // Set transactionId to variable for further use
  if(res.body.requestId) {
  bru.setEnvVar("nmpbreqId", res.body.requestId);
  }
  
  if(res.body.responseId) {
  bru.setEnvVar("nmpbrespId", res.body.responseId);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
