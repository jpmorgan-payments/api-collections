meta {
  name: NEW_EXPIRY_Override
  type: http
  seq: 8
}

post {
  url: {{accountupdater_url}}/account-updates
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{request-id}}
  merchant-id: {{merchant-id}}
}

body:json {
  {
    "accountInformation": {
      "cardNumber": "6011111111111117",
      "expiry": {
        "month": 8,
        "year": 2026
      }
    },
    "cardAccountAction": "UNREGISTER"
  }
}

tests {
  // Set transactionId to variable for further use
  if(res.body.requestId) {
  bru.setEnvVar("neourreqId", res.body.requestId);
  }
  
  if(res.body.responseId) {
  bru.setEnvVar("neourrespId", res.body.responseId);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
