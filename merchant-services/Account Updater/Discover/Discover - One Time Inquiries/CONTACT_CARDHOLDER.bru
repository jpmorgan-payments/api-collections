meta {
  name: CONTACT_CARDHOLDER
  type: http
  seq: 6
}

post {
  url: {{accountupdater_url}}/account-updates
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{request-id}}
  merchant-id: {{merchant-id}}
}

body:json {
  {
    "accountInformation": {
      "cardNumber": "6011111111111115",
      "expiry": {
        "month": 8,
        "year": 2026
      }
    }
  }
}

tests {
  // Set transactionId to variable for further use
  if(res.body.requestId) {
  bru.setEnvVar("ccotqreqId", res.body.requestId);
  }
  
  if(res.body.responseId) {
  bru.setEnvVar("ccotqrespId", res.body.responseId);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
