meta {
  name: MATCH_NO_UPDATE
  type: http
  seq: 1
}

post {
  url: {{accountupdater_url}}/account-updates
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{request-id}}
  merchant-id: {{merchant-id}}
}

body:json {
  {
    "accountInformation": {
      "cardNumber": "6011111111111110",
      "expiry": {
        "month": 8,
        "year": 2026
      }
    }
  }
}

tests {
  // Set transactionId to variable for further use
  if(res.body.requestId) {
  bru.setEnvVar("mnuotqreqId", res.body.requestId);
  }
  
  if(res.body.responseId) {
  bru.setEnvVar("mnuotqrespId", res.body.responseId);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
