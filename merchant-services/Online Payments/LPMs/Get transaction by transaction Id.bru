meta {
  name: Get transaction by transaction Id
  type: http
  seq: 82
}

get {
  url: {{online_url}}/payments/{{LPMtransactionId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{request-id}}
  merchant-id: {{merchant-id}}
}

auth:bearer {
  token: {{auth-token}}
}
