meta {
  name: Get Fraud Check Details by RequestID
  type: http
  seq: 9
}

get {
  url: {{online_url}}/fraudcheck?requestId={{fraudRequestId}}
  body: json
  auth: inherit
}

params:query {
  requestId: {{fraudRequestId}}
}

headers {
  Content-Type: application/json
  request-id: {{requestId}}
  merchant-id: {{merchantId}}
}

auth:bearer {
  token: {{auth-token}}
}
