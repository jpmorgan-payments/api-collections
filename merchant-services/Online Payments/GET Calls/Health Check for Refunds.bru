meta {
  name: Health Check for Refunds
  type: http
  seq: 4
}

get {
  url: {{online_url}}/healthcheck/refunds
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{requestId}}
  merchant-id: {{merchantId}}
}

auth:bearer {
  token: {{auth-token}}
}
