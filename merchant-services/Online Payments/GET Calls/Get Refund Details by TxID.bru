meta {
  name: Get Refund Details by TxID
  type: http
  seq: 3
}

get {
  url: {{online_url}}/refunds/{{transactionId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{requestId}}
  merchant-id: {{merchantId}}
}

auth:bearer {
  token: {{auth-token}}
}
