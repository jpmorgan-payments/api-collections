meta {
  name: Get Fraud Check Details by TxID
  type: http
  seq: 7
}

get {
  url: {{online_url}}/fraudcheck/{{transactionId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  request-id: {{requestId}}
  merchant-id: {{merchantId}}
}

auth:bearer {
  token: {{auth-token}}
}
